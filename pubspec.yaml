name: atoupic
description: A belote card game

version: 0.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  flame: ^0.18.1
  kiwi: ^0.2.0
  equatable: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1
  build_runner: ^1.7.2
  kiwi_generator: ^0.4.0

flutter:
  uses-material-design: true
